utterance:
  - ${condition}
  - ${sequence}
  - ${simple}

simple:
  - ${command}

command:
  - ${events_command}
  - ${home_command}
  - ${messages_command}
  - ${music_command}
  - ${navigation_command}
  - ${products_command}
  - ${reminders_command}
  - ${weather_command}
